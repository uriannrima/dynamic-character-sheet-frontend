export const Classe = function ({ name, level, isCaster, casterAbility, castingBonusAbility }) {
    return {
        name,
        level,
        isCaster,
        casterAbility,
        castingBonusAbility
    }
}
