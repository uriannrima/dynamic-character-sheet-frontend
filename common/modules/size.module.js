exports.Size = function ({ _id, name, attackDCBonus, grappleBonus, hideBonus, carryBonus }) {
    return {
        _id,
        name,
        attackDCBonus,
        grappleBonus,
        hideBonus,
        carryBonus
    }
}